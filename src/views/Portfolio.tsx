import { View } from "../components";

/** The main page of the app. */
const Portfolio = () => {
  return <View headerText="Portfolio">Home page</View>;
};

/** Exports */

export default Portfolio;
